{
  "name": "alpha-empire-logistics",
  "private": true,
  "workspaces": ["apps/*", "tools"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web\" \"npm run dev --workspace=apps/pocketbase\"",
    "build": "npm run build --workspace=apps/web",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
